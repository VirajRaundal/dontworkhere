# Lean dependency set for the Vercel serverless function (server.py + app/).
# Intentionally minimal — the full backend/requirements.txt is the Emergent
# image's list and is far too large for a serverless bundle.
fastapi==0.110.1
motor==3.3.1
pymongo==4.5.0
pydantic==2.13.4
email-validator==2.3.0
python-dotenv==1.2.1
httpx==0.28.1
pillow==11.3.0
